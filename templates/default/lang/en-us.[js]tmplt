import { SCOPES } from '@devitools/Agnostic/enum'

const parent = '{{namespace.singular}}'
const singular = '{{entity.singular}}'
const plural = '{{entity.plural}}'

export const routes = {
  group: {
    crumb: `${parent} / ${plural}`
  },
  [SCOPES.SCOPE_INDEX]: {
    title: plural
  },
  [SCOPES.SCOPE_TRASH]: {
    title: `${plural} Trash`,
    crumb: 'Trash'
  },
  [SCOPES.SCOPE_ADD]: {
    title: `Create ${singular}`,
    crumb: 'Create'
  },
  [SCOPES.SCOPE_VIEW]: {
    title: `View ${singular}`,
    crumb: 'View'
  },
  [SCOPES.SCOPE_EDIT]: {
    title: `Edit ${singular}`,
    crumb: 'Edit'
  }
}

export const print = {
  title: `${singular} Impression`
}

export const fields = {
  name: {
    label: 'ex.: Name',
    tooltip: 'ex.: The name used to represent the record',
    placeholder: 'ex: Type a cool name'
  }
}

export const validations = {
  name: {
    requiredIf: 'ex.: Name is required'
  }
}

/**
 */
export default {
  routes,
  print,
  fields,
  validations
}
